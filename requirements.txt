#
# Hand-maintained pinned dependencies for the pyxtermjs terminal server.
#
# Originally generated by pip-compile, but edited directly since (eventlet
# was added by hand). requirements.in has been removed: regenerating from it
# would have dropped eventlet, which flask-socketio needs as its async
# backend. Edit the pins below directly.
#
bidict==0.21.2
    # via python-socketio
click==8.0.1
    # via flask
flask==2.0.1
flask_cors==3.0.10
    # via flask-socketio
flask-socketio==5.1.1
itsdangerous==2.0.1
    # via flask
jinja2==3.0.1
    # via flask
markupsafe==2.0.1
    # via jinja2
python-engineio==4.2.1
    # via python-socketio
python-socketio==5.4.0
    # via flask-socketio
werkzeug==2.0.1
    # via flask
eventlet==0.33.3
    # via flask-socketio (async backend)
